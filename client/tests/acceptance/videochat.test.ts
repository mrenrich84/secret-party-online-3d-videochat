// videochat app
    // when launched
        // open the camera
        // start 3D world
            // start the network layer

describe('videochat app', () => {
    describe('when launched', () => {
           it('should try open the camera', () => {})
    });
});